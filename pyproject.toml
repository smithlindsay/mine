[tool.poetry]
name = "mine"
version = "0.1.0"
description = "MINE :)"
authors = ["Lindsay Smith <lindsay.smith@princeton.edu>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
